# UI plans / карта текущего интерфейса

## 1) Где лежит главный layout
- Главная HTML-разметка интерфейса находится в `frontend/index.html`.
- Корневой контейнер приложения: `.wrap > .card`.
- Основная структура экрана: `.top` (верхняя панель) + `.grid` (sidebar + чат-область).
- Точки монтирования ключевых блоков:
  - список чатов: `#chatlist`
  - лента сообщений: `#msgs`
  - composer (поле и кнопки отправки/вложений): `.composer`

## 2) Где стили
- Основные стили интерфейса: `frontend/styles.css`.
- В проекте не используется Tailwind; стили заданы обычным CSS.
- Адаптивность и mobile-поведение описаны через media queries (`max-width: 640px`, `max-width: 900px`, `min-width: 901px`).

## 3) Где логика app / components
- Основная клиентская логика находится в `frontend/app.js` (единый файл).
- Инициализация и управление UI (темы, drawer, модалки, чат-лист, сообщения, composer, websocket-события) также находятся в `frontend/app.js`.

## 4) Где модалки / меню / чат-лист / сообщения / composer
- **Меню и навигация**
  - верхнее меню пользователя: `#profileMenu` в `frontend/index.html`
  - мобильный drawer/сайдбар: `#sidebar`, фон: `#drawerBackdrop`
- **Модалки и оверлеи**
  - авторизация: `#authOverlay`
  - создание (sheet): `#sheetOverlay`
  - профиль: `#profileOverlay`
  - контакты: `#contactsOverlay`
  - инфо чата: `#chatInfoOverlay`
  - профиль пользователя: `#userProfileOverlay`
  - просмотр медиа: `#mediaViewerOverlay`
  - предпросмотр голосового: `#voicePreviewOverlay`
  - звонок: `#callOverlay`
- **Чат-лист**
  - контейнер: `#chatlist`
  - рендер и фильтрация: функции `getFilteredChats()` и `renderChatList()` в `frontend/app.js`
- **Сообщения**
  - контейнер: `#msgs`
  - загрузка/пагинация: `loadHistory()` и `loadOlderMessages()`
  - рендер сообщений и реакций: функции в `frontend/app.js` (`addMsg`, `renderReactions`, и связанные)
- **Composer**
  - контейнер: `.composer` в `frontend/index.html`
  - отправка: функция `send()` в `frontend/app.js`

## 5) Состояния UI (loading / error / empty)
- **Loading**
  - загрузка сообщений при открытии чата: `loadHistory()` + системный toast `📥 Loading...`
  - загрузка более старых сообщений: `loadOlderMessages()`
- **Error**
  - сетевые ошибки показываются через `showNetworkError(...)`
  - ошибки загрузки чатов/сообщений отображаются через системные сообщения `❌ ...`
  - офлайн-режим: баннер `#offlineBanner`
- **Empty**
  - пустой чат-лист: `Нет чатов. Создай ➕ или 💬`
  - пустой результат поиска по чатам: `Ничего не найдено`
  - чат без сообщений: `Нет сообщений`
  - пустые вкладки в инфо чата (медиа/файлы/голосовые/ссылки)

## 6) Manual checklist UI (desktop + mobile)
- [ ] Открыть `/` и убедиться, что layout загружается без ошибок в консоли.
- [ ] Desktop (широкий экран):
  - [ ] Видны top bar, sidebar, чат-область, composer.
  - [ ] Кнопки верхней панели (`Войти`, `Тема`, `Приложение`) кликабельны.
  - [ ] Фильтры чатов (`Все/Личные/Группы/Новые`) переключаются.
- [ ] Mobile (узкий экран ≤ 900px):
  - [ ] Sidebar открывается/закрывается через кнопку `☰` и backdrop.
  - [ ] После выбора чата sidebar закрывается.
  - [ ] Composer остаётся доступен и не выходит за границы экрана.
- [ ] Модалки/листы открываются и закрываются (Auth, Contacts, Profile, Chat Info).
- [ ] Проверить, что статусные элементы (`#status`, `#net`, offline banner) отображаются корректно.
- [ ] Регрессии базовых сценариев:
  - [ ] Login/Logout
  - [ ] Список чатов
  - [ ] Отправка сообщения
  - [ ] WebSocket-подключение (индикатор сети, получение новых сообщений)
